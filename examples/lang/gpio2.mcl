import "fmt"
import "gpio"

# read the switch on pin 0; gpio.pin polls and sends a new event on each change
$b = gpio.pin(0)

file "/tmp/pin0" {
	state => "exists",
	content => fmt.printf("value: %t\n", $b),
}

#gpio "led1" { # XXX: name should be dev+pin ?
#	pin => 1,
#	value => $b,
#}
